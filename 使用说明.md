# AutoMC 简明使用教程

## 1. 下载并打开

下载 `AutoMC-v版本号.zip`，完整解压到固定目录，双击最外层 `AutoMC.exe`，点击“启动 AutoMC”。不要在压缩包里直接运行，也不要下载 Source code 当程序。

当前完整包是 AutoMC 1.5.7，管理器是 Manager 1.0.3。两者独立更新，版本号不同是正常的。

每日任务页面可勾选“每日任务失败后自动重试一次”，保存后生效，默认关闭。仅每日执行失败且未确认所有账号活跃度达100时整轮重跑一次；手动停止不重试。所有账号已达标但附加任务失败，报告为成功但有异常，并保留失败详情。

## 2. 基础配置

提前安装官服鸣潮、OK-WW，手动登录游戏，确认 OK-WW 能使用。在 AutoMC 的“基础配置”中检测或选择两个安装目录并保存。

DX11、DX12 均允许尝试，无需为 AutoMC 特意切换图形接口；游戏进入、窗口识别等其他条件仍须满足。

## 3. 账号模式

在“每日任务”的“选择运行方式”中选择：

- 单账号：只完成当前登录账号；不自动选列表第一项，不切换下一个号，不执行“下次优先登录账号”。
- 多账号：使用 OK-WW 原生多账号每日流程。

分账号列表来自此前识别记录和已有配置。没有记录时可先配置默认任务，不能凭空识别未出现过的账号。

## 4. 刷本和附加任务

“任务参数设置”默认折叠；底部操作栏始终可见。展开后，先选择“默认任务配置”或某个账号。参数选择“单独设置此项”才覆盖；开关直接选择“沿用 / 开启 / 关闭”。沿用会显示默认配置或 OK-WW 的值，未读取时明确提示，不代表关闭。

| 体力用途 | 参数 |
|---|---|
| 无音区 | 游戏 F2 列表序号，从 1 开始 |
| 凝素领域 | 游戏 F2 列表序号，从 1 开始 |
| 模拟领域 | 角色经验、武器经验或贝币 |

选择用途后显示相应参数，其他类型的参数保留但不同时执行。地图、传送点和副本须已解锁。

附加任务可多选：每周乐园、全部梦魇巢穴、弃置声骸超过 1000 时融合、传送并刷取 4C。是否需要执行由 OK-WW 按日常完成状态等条件判断，勾选不代表每次一定执行。

### 梦魇巢穴

展开详细参数，可选“梦魇拔除”“残像聚落”。开启全部巢穴时至少选一类目标。

“必要时通过梦魇巢穴获取日常声骸”和“刷全部巢穴”不同。梦魇任务按 OK-WW 原生顺序在消耗体力前执行。

### 4C 声骸

展开详细参数，设置传送类别、目标序号、等级、BOSS 配置、次数、等待、拾取方式、共鸣解放和治疗角色选项。

新配置默认 **20 次**。导入已有配置保留导入次数，请查看预览。作为每日附加任务时，必须选择“周本挑战”或“BOSS 挑战”，不能选择“不传送”。

最后点击 **保存所有设置**。未保存的编辑不影响下次运行。

## 5. 导入和分账号配置

优先级：**分账号覆盖 > AutoMC 默认 > OK-WW 原配置**。

例如默认刷无音区第 3 项，账号 A 单独设凝素领域第 2 项，账号 B 无覆盖。A 刷凝素，B 刷无音区。

导入步骤：

1. 选择“默认配置”或某个账号。
2. 点击“从 OK-WW 导入配置”，查看预览。
3. 确认后只填入当前范围，不影响其他账号的单独覆盖。
4. 点击保存才生效；跟随默认的账号会采用新默认值。

配置存到 AutoMC，不写回 OK-WW。两边不是双向同步；AutoMC 已覆盖的参数不会随 OK-WW 编辑自动变化，需要重新导入或取消覆盖。

## 6. 通知与结束行为

“通知”里可用个人 PushPlus Token，或受邀微信授权；受邀模式仅微信。报告可选 HTML / 纯文本，默认 HTML。

普通用户不需要管理员密钥。不要把 Token、兑换码、管理密钥发在截图或公开问题中。

首次运行前检查“任务完成后关闭鸣潮”和“自动关机”。退出和关机由 AutoMC 管理，不依赖 OK-WW 的“完成后退出”。

## 7. 立即运行与定时运行

点击“立即运行”，确认显示的已保存配置。运行时不要手动操作游戏或再开另一份 OK-WW 执行任务。

手动确认符合预期后，设置时间并点击“开启每日自动运行”，按提示确认管理员权限。电脑需要开机、用户已登录且桌面可交互；锁屏、睡眠或远程会话变化可能影响游戏操作。

修改时间后保存；移动软件目录或换电脑后，从新目录重新安装一次计划任务。

## 8. 更新、备份与换电脑

从“版本与更新”返回管理器，安装或切换可用 Core。Manager 自动检查自身更新，更新不要求重新配置所有任务。

设置在 `%LOCALAPPDATA%\AutoMC\settings.json`，运行记录在同目录的 `runtime`。软件包不含私人配置。

换电脑先导出配置，新电脑导入后重新选择路径、设置通知授权、安装计划任务。受邀授权绑定 Windows 安装，不能复制授权文件跨电脑使用。未确认备份前不要删除用户数据目录。

## 9. 常见问题

- 版本列表失败：仍可启动本地 Core；稍后刷新，下载慢时切换国内源。
- 设置没生效：检查是否保存、是否选对账号、参数是否选择了单独设置。
- 导入后默认值变化：跟随默认的账号会变化，单独覆盖应保持。
- 没按时运行：检查电脑状态及计划任务路径。
- 运行失败：在“诊断”查看最近结果或运行记录，提供时间、软件版本和错误信息。

新增任务选项未代替实机验证；首次请手动运行观察，再启用无人值守。

### 每日任务页操作说明

按顺序选择账号模式、运行时间、结束行为。任务参数按需展开，底部“保存所有设置”统一保存整页（包括折叠参数）。有未保存修改时会提示。

“开启每日自动运行”会先保存，再创建 Windows 计划任务；已经开启时显示“保存并更新自动运行”。“取消每日自动运行”只移除计划任务，不删除账号或参数。

凝素领域仍按游戏 F2 序号定位。旁边按迅刀、音感仪、长刃、臂铠、配枪每五项循环提示类型和组别；这是排列参考，不保证未来版本或新旧材料顺序，具体以游戏列表为准。模拟领域直接选择角色经验、武器经验或贝币。
